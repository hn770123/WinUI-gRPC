<Page
    x:Class="ChatApp.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Grid Grid.Row="0" Background="{ThemeResource SystemControlAcrylicElementBrush}" Padding="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="← 戻る" Command="{x:Bind ViewModel.GoBackCommand}" Margin="0,0,16,0"/>
            <TextBlock Grid.Column="1" Text="ユーザー管理" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="更新" Command="{x:Bind ViewModel.LoadUsersCommand}"/>
        </Grid>

        <!-- ユーザー一覧 -->
        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel MaxWidth="800">
                <ListView ItemsSource="{x:Bind ViewModel.Users, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:User">
                            <Grid Padding="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="18" FontWeight="SemiBold">
                                        <Run Text="{x:Bind DisplayName}"/>
                                        <Run Text="(@" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        <Run Text="{x:Bind Username}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        <Run Text=")" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                    </TextBlock>
                                    <TextBlock Text="{x:Bind Id}"
                                               FontSize="12"
                                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind IsActive, Converter={StaticResource ActiveStatusConverter}}"
                                               Foreground="{x:Bind IsActive, Converter={StaticResource ActiveStatusColorConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
